{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main window. The clipboard plugin is deliberately absent: copies are performed in Rust so plaintext never reaches the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
